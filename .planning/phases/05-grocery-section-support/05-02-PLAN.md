---
phase: 05-grocery-section-support
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - TOOLS.md
  - QUICKSTART.md
autonomous: true
requirements: [GROC-01, GROC-02, GROC-03]

must_haves:
  truths:
    - "Agent knows how to add grocery items to specific Todoist sections using the section parameter"
    - "Agent knows how to list available sections before adding items"
    - "Agent can extract ingredient names from a recipe and add them to the grocery list without quantities"
  artifacts:
    - path: "TOOLS.md"
      provides: "Grocery section documentation and recipe-to-grocery workflow"
      contains: "section"
    - path: "QUICKSTART.md"
      provides: "Quick reference for grocery sections"
      contains: "section"
  key_links:
    - from: "TOOLS.md"
      to: "tasks/todoist/config.js"
      via: "CLI command examples"
      pattern: "todoist.*section"
    - from: "TOOLS.md"
      to: "household/meals/recipes/"
      via: "recipe ingredient extraction instructions"
      pattern: "recipes/"
---

<objective>
Document the grocery section workflow and recipe-to-grocery ingredient extraction in agent instructions so the agent reliably uses sections and can add recipe ingredients to the grocery list.

Purpose: GROC-01/02 need agent-facing documentation for the new section commands. GROC-03 requires instructions for extracting ingredient names (no quantities) from recipe files and adding them to the Todoist shopping project with correct section placement.

Output: Updated TOOLS.md with grocery section documentation and recipe-to-grocery workflow. Updated QUICKSTART.md with section quick reference.
</objective>

<execution_context>
@/Users/greg/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-grocery-section-support/05-01-SUMMARY.md
@TOOLS.md
@QUICKSTART.md
@household/meals/recipes/chicken-parmesan.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add grocery section documentation and recipe-to-grocery workflow to TOOLS.md</name>
  <files>
    TOOLS.md
  </files>
  <action>
1. Find the existing "### Shopping (Todoist)" section in TOOLS.md (around line 181).

2. After the existing shopping section content, add a new subsection "#### Grocery Sections" with:
   - Explanation: shopping items can be placed in specific Todoist sections (Produce, Dairy, Meat, etc.)
   - **List sections command:** `node tasks/index.js "todoist sections project=shopping"` -- always list sections first to get section IDs
   - **Add item to section:** `node tasks/index.js "todoist add project=shopping content=Milk section_id=SECTION_ID"`
   - Workflow: (1) list sections to get IDs, (2) match item to section by name, (3) add with section_id
   - If no matching section exists, add without section_id (item goes to project root -- still valid)
   - DO: always list sections first to get current IDs (they may change)
   - DO NOT: hardcode section IDs, guess section IDs, create new sections

3. After the Grocery Sections subsection, add "#### Recipe to Grocery List" with:
   - Trigger phrases: "add ingredients from [recipe]", "grocery list from [recipe]", "shop for [recipe]"
   - Workflow:
     1. Find recipe file in `household/meals/recipes/` (match by name, case-insensitive)
     2. Read the `## Ingredients` section
     3. Extract item names only -- strip quantities, units, and preparation notes (e.g., "4 boneless skinless chicken breasts" becomes "chicken breasts", "1 cup marinara sauce" becomes "marinara sauce", "2 eggs, beaten" becomes "eggs")
     4. List Todoist sections: `node tasks/index.js "todoist sections project=shopping"`
     5. For each ingredient, match to the best section (Produce, Dairy, Meat, etc.) using common sense. If unsure, add without section.
     6. Add each item: `node tasks/index.js "todoist add project=shopping content=ITEM section_id=SECTION_ID"`
   - Quantity stripping rules: remove leading numbers, fractions, units (cup, cups, tablespoon, tablespoons, tbsp, tsp, teaspoon, teaspoons, oz, ounce, ounces, lb, pound, pounds, clove, cloves). Remove trailing preparation phrases after comma (", beaten", ", diced", ", chopped").
   - Example: Given chicken-parmesan.md ingredients, extract: chicken breasts, breadcrumbs, Parmesan cheese, eggs, marinara sauce, mozzarella cheese, olive oil, salt, pepper
   - DO: Strip quantities and units. Keep the item name descriptive enough to shop for.
   - DO NOT: Include quantities (user said "just item names"). Skip salt/pepper/oil if user requests "skip pantry staples".
   - Response format: "Added X ingredients from [recipe name] to the shopping list: [list items]"

4. Add a DO/DO NOT row to the existing Shopping DO/DO NOT table:
   - DO: "List sections before adding items to get current section IDs"
   - DO NOT: "Hardcode or guess section IDs"
  </action>
  <verify>
    Grep TOOLS.md for "Grocery Sections" -- should find the new subsection.
    Grep TOOLS.md for "Recipe to Grocery" -- should find the workflow.
    Grep TOOLS.md for "section_id" -- should find command examples.
    Grep TOOLS.md for "todoist sections" -- should find list command.
  </verify>
  <done>
    TOOLS.md has Grocery Sections documentation with list/add commands, and Recipe to Grocery List workflow with ingredient extraction rules and examples.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add grocery section quick reference to QUICKSTART.md</name>
  <files>
    QUICKSTART.md
  </files>
  <action>
1. Find the existing Shopping section in QUICKSTART.md.

2. Add a brief (4-6 lines max) "Grocery Sections" note after the shopping quick reference:
   - List sections: `node tasks/index.js "todoist sections project=shopping"`
   - Add to section: `node tasks/index.js "todoist add project=shopping content=Milk section_id=SECTION_ID"`
   - Recipe to grocery: read recipe from `household/meals/recipes/`, extract item names (no quantities), add to shopping with sections

Keep it under 8 lines per the established convention (from Phase 03 decisions) to preserve context efficiency for cheap LLMs.
  </action>
  <verify>
    Grep QUICKSTART.md for "section" -- should find the new reference.
    Count lines in the new section -- should be 8 or fewer.
  </verify>
  <done>
    QUICKSTART.md has grocery sections quick reference, under 8 lines, with list/add/recipe commands.
  </done>
</task>

</tasks>

<verification>
1. TOOLS.md contains "Grocery Sections" subsection with list and add commands
2. TOOLS.md contains "Recipe to Grocery List" subsection with extraction workflow
3. TOOLS.md has section_id in command examples
4. QUICKSTART.md has grocery sections reference under 8 lines
5. No existing TOOLS.md or QUICKSTART.md content was removed or broken
</verification>

<success_criteria>
- Agent instructions document how to list sections, add items to sections, and extract recipe ingredients
- Ingredient extraction rules specify quantity stripping and item-name-only output
- QUICKSTART.md has concise section reference
- All existing documentation preserved
</success_criteria>

<output>
After completion, create `.planning/phases/05-grocery-section-support/05-02-SUMMARY.md`
</output>
