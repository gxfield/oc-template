---
phase: 05-grocery-section-support
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tasks/todoist/helpers/list-sections.js
  - tasks/todoist/helpers/create-task.js
  - tasks/todoist/config.js
autonomous: true
requirements: [GROC-01, GROC-02]

must_haves:
  truths:
    - "Agent can add a grocery item to a specific Todoist section by passing section parameter"
    - "Agent can list all available sections in the Todoist shopping project"
    - "Creating a task without section parameter still works exactly as before"
  artifacts:
    - path: "tasks/todoist/helpers/list-sections.js"
      provides: "Fetch sections from Todoist project via REST API"
      exports: ["listSections"]
    - path: "tasks/todoist/helpers/create-task.js"
      provides: "Task creation with optional section_id"
      exports: ["createTask"]
    - path: "tasks/todoist/config.js"
      provides: "Todoist task config with sections intent"
  key_links:
    - from: "tasks/todoist/helpers/list-sections.js"
      to: "Todoist REST API GET /api/v1/sections"
      via: "todoistRequest helper"
      pattern: "todoistRequest.*GET.*sections"
    - from: "tasks/todoist/helpers/create-task.js"
      to: "Todoist REST API POST /api/v1/tasks"
      via: "section_id in request body"
      pattern: "section_id"
    - from: "tasks/todoist/config.js"
      to: "tasks/todoist/helpers/list-sections.js"
      via: "require and intent registration"
      pattern: "listSections"
---

<objective>
Add Todoist section support to the task system so items can be placed in specific grocery store sections (Produce, Dairy, etc.) and sections can be listed.

Purpose: GROC-01 and GROC-02 require the agent to place grocery items into Todoist sections and list available sections. The Todoist REST API v1 supports `section_id` on task creation and `GET /api/v1/sections` for listing.

Output: Working `list-sections.js` helper, updated `create-task.js` with section support, updated todoist config with `sections` intent.
</objective>

<execution_context>
@/Users/greg/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@tasks/todoist/helpers/todoist-api.js
@tasks/todoist/helpers/create-task.js
@tasks/todoist/helpers/fetch-tasks.js
@tasks/todoist/config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create list-sections helper and add section_id support to create-task</name>
  <files>
    tasks/todoist/helpers/list-sections.js
    tasks/todoist/helpers/create-task.js
  </files>
  <action>
1. Create `tasks/todoist/helpers/list-sections.js`:
   - Follow the exact same pattern as `fetch-tasks.js` (same imports, same structure)
   - Export `listSections(parameters, context)` function
   - Accept `parameters.project` (default: `'shopping'`) to resolve project ID from credentials
   - Call `todoistRequest('GET', '/api/v1/sections?project_id=${projectId}', apiKey)`
   - Handle paginated response: `response.results || response` (same pattern as fetch-tasks.js line 24)
   - Return `{ sections: [{id, name, order}], count, project }` mapping each section to `{ id: s.id, name: s.name, order: s.section_order }`

2. Update `tasks/todoist/helpers/create-task.js`:
   - Add `section_id` support: if `parameters.section_id` is provided, include `section_id: parameters.section_id` in the request body object
   - Add it alongside existing optional fields (description, due_string, priority) using the same `if (parameters.section_id) body.section_id = parameters.section_id;` pattern
   - Include `section_id` in the returned task object: `section_id: task.section_id || null`
   - No other changes -- backward compatible
  </action>
  <verify>
    Run `node tasks/index.js "todoist sections project=shopping"` -- should return JSON with sections array (or error if no credentials, but code structure should be valid).
    Run `node -e "require('./tasks/todoist/helpers/list-sections')"` -- should load without errors.
    Run `node -e "require('./tasks/todoist/helpers/create-task')"` -- should load without errors.
  </verify>
  <done>
    list-sections.js exports listSections function that calls Todoist sections API. create-task.js accepts optional section_id and passes it to the API. Both load without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register sections intent in todoist config</name>
  <files>
    tasks/todoist/config.js
  </files>
  <action>
1. In `tasks/todoist/config.js`:
   - Add `const { listSections } = require('./helpers/list-sections');` at top alongside existing imports
   - Add new intent `'sections'` to the `intents` object:
     ```
     'sections': {
       helpers: ['listSections']
     }
     ```
   - Add `'listSections': listSections` to the `helpers` object
   - No changes to existing intents -- backward compatible

This enables: `node tasks/index.js "todoist sections project=shopping"` to list all sections.
  </action>
  <verify>
    Run `node -e "const c = require('./tasks/todoist/config'); console.log(Object.keys(c.intents))"` -- should print `['get', 'add', 'done', 'sections']`.
    Run `node -e "const c = require('./tasks/todoist/config'); console.log(typeof c.helpers.listSections)"` -- should print `function`.
  </verify>
  <done>
    Todoist config has `sections` intent registered with `listSections` helper. Existing intents (get, add, done) unchanged.
  </done>
</task>

</tasks>

<verification>
1. `node -e "require('./tasks/todoist/helpers/list-sections')"` loads without error
2. `node -e "require('./tasks/todoist/helpers/create-task')"` loads without error
3. `node -e "const c = require('./tasks/todoist/config'); console.log(Object.keys(c.intents))"` prints all 4 intents
4. `node tasks/index.js "todoist sections project=shopping"` returns sections or auth error (not a code error)
5. Existing `node tasks/index.js "todoist get project=shopping"` still works unchanged
</verification>

<success_criteria>
- list-sections.js exists and exports listSections
- create-task.js accepts optional section_id parameter
- todoist config.js has sections intent registered
- All existing todoist functionality (get, add, done) unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/05-grocery-section-support/05-01-SUMMARY.md`
</output>
