---
phase: 04-recipe-library
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - household/meals/recipes/chicken-parmesan.md
  - TOOLS.md
  - QUICKSTART.md
  - AGENTS.md
autonomous: true
requirements:
  - RCPE-01
  - RCPE-02
  - RCPE-03

must_haves:
  truths:
    - "A recipes/ directory exists under household/meals/ with at least one valid markdown recipe file"
    - "Agent can list all available recipes by name and tags"
    - "Agent can read a specific recipe and return ingredients and instructions"
    - "Agent can create a new recipe file from user dictation"
    - "Agent can suggest recipes filtered by tag (e.g., 'suggest a chicken dinner')"
    - "Recipe file uses YAML frontmatter with title, servings, prep_time, cook_time, tags"
  artifacts:
    - path: "household/meals/recipes/chicken-parmesan.md"
      provides: "Sample recipe in standard format"
      contains: "title:"
    - path: "TOOLS.md"
      provides: "Recipe Library section with browse, read, create, suggest commands"
      contains: "Recipe Library"
    - path: "QUICKSTART.md"
      provides: "Recipe Library quick reference"
      contains: "Recipe"
    - path: "AGENTS.md"
      provides: "Recipe Library mention in Step 4 tool loading"
      contains: "Recipe Library"
  key_links:
    - from: "TOOLS.md"
      to: "household/meals/recipes/"
      via: "Recipe Library section references directory and file format"
      pattern: "household/meals/recipes"
    - from: "QUICKSTART.md"
      to: "TOOLS.md"
      via: "Quick reference points to full Recipe Library docs"
      pattern: "Recipe"
---

<objective>
Create the recipe library infrastructure: standard recipe markdown format, sample recipe, and agent instructions for browsing, reading, creating, and suggesting recipes.

Purpose: Enable the agent to work with structured recipe files -- list them, read them, create new ones from dictation, and suggest recipes by tag.
Output: recipes/ directory with sample file, TOOLS.md Recipe Library section, QUICKSTART.md entry, AGENTS.md Step 4 mention.
</objective>

<execution_context>
@/Users/greg/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-recipe-library/04-CONTEXT.md
@TOOLS.md
@QUICKSTART.md
@AGENTS.md
@household/meals/this-week.md
@household/meals/favourites.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create recipe directory and sample recipe file</name>
  <files>household/meals/recipes/chicken-parmesan.md</files>
  <action>
Create the directory `household/meals/recipes/` and add one sample recipe file `chicken-parmesan.md` using this exact format:

```markdown
---
title: Chicken Parmesan
servings: 4
prep_time: 15 min
cook_time: 30 min
tags:
  - chicken
  - italian
  - quick
---

## Ingredients

- 4 boneless skinless chicken breasts
- 1 cup breadcrumbs
- 1/2 cup grated Parmesan cheese
- 2 eggs, beaten
- 1 cup marinara sauce
- 1 cup shredded mozzarella cheese
- 2 tablespoons olive oil
- Salt and pepper to taste

## Instructions

1. Preheat oven to 400°F
2. Season chicken breasts with salt and pepper
3. Dip chicken in beaten eggs, then coat in breadcrumb and Parmesan mixture
4. Heat olive oil in an oven-safe skillet over medium-high heat
5. Sear chicken 3-4 minutes per side until golden
6. Top each breast with marinara sauce and mozzarella
7. Transfer to oven and bake 15-20 minutes until chicken reaches 165°F
8. Let rest 5 minutes before serving
```

This format establishes:
- YAML frontmatter: `title`, `servings`, `prep_time`, `cook_time`, `tags` (array of freeform strings)
- `## Ingredients` section with `- quantity unit item` lines
- `## Instructions` section with numbered steps
- File naming: kebab-case matching the recipe title (e.g., `chicken-parmesan.md`)

Tags are freeform strings per user decision. The format is clean for both human authoring and agent parsing.
  </action>
  <verify>
Confirm file exists at `household/meals/recipes/chicken-parmesan.md`. Read it back and verify:
1. YAML frontmatter contains title, servings, prep_time, cook_time, tags
2. Ingredients section has bullet items
3. Instructions section has numbered steps
  </verify>
  <done>Recipe directory exists with one valid sample recipe in the standard format</done>
</task>

<task type="auto">
  <name>Task 2: Add Recipe Library section to TOOLS.md, QUICKSTART.md, and AGENTS.md</name>
  <files>TOOLS.md, QUICKSTART.md, AGENTS.md</files>
  <action>
**TOOLS.md:** Add a new `## Recipe Library` section AFTER the "Save Recipe" section and BEFORE the "Telegram Polls" section. This section documents:

1. **Recipe File Format** — the YAML frontmatter fields (title, servings, prep_time, cook_time, tags) and section structure (## Ingredients, ## Instructions). Reference `household/meals/recipes/` as the directory.

2. **Trigger Phrase Table:**

| Trigger Pattern | Action |
|---|---|
| "show recipes", "list recipes", "what recipes do we have" | List all recipe names + tags |
| "show recipe [name]", "get recipe for [name]" | Display full recipe (Telegram-formatted) |
| "suggest a [tag] dinner", "what [tag] recipes do we have" | Filter recipes by tag match |
| "save recipe [dictation]" → "create recipe [dictation]" | Create new recipe file from dictation (see below) |

3. **Browsing (List View):**
- Read all `.md` files in `household/meals/recipes/`
- For each file, parse YAML frontmatter to get title and tags
- Display as: `- {title} — {tag1}, {tag2}, ...`
- If no recipes found: "No recipes in the library yet!"

4. **Reading (Full View):**
- Find recipe file by matching title (case-insensitive, partial match OK)
- Display Telegram-formatted output:
```
🍽️ {title}
Servings: {servings} | Prep: {prep_time} | Cook: {cook_time}

📝 Ingredients:
• {ingredient1}
• {ingredient2}
...

👨‍🍳 Instructions:
1. {step1}
2. {step2}
...
```

5. **Suggesting (Tag-Based):**
- When user asks "suggest a chicken dinner" or "what quick recipes do we have"
- Read all recipe frontmatter, filter by matching tag (case-insensitive)
- Show matching recipes in list view format
- If no matches: "No recipes with that tag. Here's everything: [list all]"

6. **Creating (From Dictation):**
- User describes a recipe in chat, agent writes it as a `.md` file
- Agent must format into the standard template (YAML frontmatter + sections)
- File name: kebab-case of the title (e.g., "Beef Tacos" → `beef-tacos.md`)
- Agent should ask for missing fields if not provided (especially tags)
- If user just says "save recipe [text]", that still goes to favourites.md (existing behavior). "Create recipe" triggers the structured file creation.

7. **DO / DO NOT table:**

| DO | DO NOT | WHY |
|---|---|---|
| Read all .md files in recipes/ for listing | Hardcode recipe names | New recipes added frequently |
| Match tags case-insensitively | Require exact tag match | Users say "chicken" not "Chicken" |
| Format output for Telegram (emoji headers, bullet lists) | Use markdown tables in output | Telegram doesn't render tables |
| Create file name from title in kebab-case | Use random or numbered file names | Human-readable file names for external editing |
| Ask for tags if user doesn't provide them during dictation | Skip tags silently | Tags enable search/suggestions |

**QUICKSTART.md:** Add a "## Recipe Library" section AFTER the "Save Recipe" section and BEFORE the "Polls" section. Keep it concise (under 10 lines):

```
## Recipe Library (household/meals/recipes/)

Structured markdown recipes with YAML frontmatter (title, servings, prep_time, cook_time, tags).

| Task | Action |
|------|--------|
| List recipes | Read all `.md` in recipes/, show title + tags |
| Show recipe | Find by title match, display Telegram-formatted |
| Suggest by tag | Filter by tag: "suggest a chicken dinner" |
| Create recipe | From dictation: write `.md` file in standard format |

- Tags are freeform: "chicken", "quick", "low-carb", etc.
- "save recipe X" still goes to favourites.md (titles/URLs)
- "create recipe" writes a structured file to recipes/
```

**AGENTS.md:** In Step 4 (Load tools reference), add a bullet point for Recipe Library after the existing "Save Recipe" bullet:
`- **Recipe Library:** Agent can list, read, suggest, and create structured recipes in `household/meals/recipes/`. Tag-based suggestions for dinner ideas. See TOOLS.md Recipe Library section.`
  </action>
  <verify>
1. Read TOOLS.md and confirm Recipe Library section exists with trigger table, format docs, DO/DO NOT table
2. Read QUICKSTART.md and confirm Recipe Library section exists
3. Read AGENTS.md Step 4 and confirm Recipe Library bullet exists
4. Verify the "Save Recipe" section in TOOLS.md is unchanged (favourites.md flow preserved)
  </verify>
  <done>TOOLS.md has comprehensive Recipe Library section, QUICKSTART.md has concise reference, AGENTS.md Step 4 mentions Recipe Library — all documenting browse, read, suggest, and create workflows</done>
</task>

</tasks>

<verification>
1. `ls household/meals/recipes/*.md` returns at least one recipe file
2. Recipe file has valid YAML frontmatter (title, servings, prep_time, cook_time, tags)
3. TOOLS.md contains "Recipe Library" section with trigger phrases, format docs, and DO/DO NOT
4. QUICKSTART.md contains "Recipe Library" section
5. AGENTS.md Step 4 mentions Recipe Library
6. Existing "Save Recipe" section in TOOLS.md is unchanged
</verification>

<success_criteria>
- Recipe directory exists with sample recipe in standard format (YAML frontmatter + Ingredients + Instructions)
- Agent instructions fully document listing, reading, suggesting, and creating recipes
- QUICKSTART.md and AGENTS.md updated for agent discoverability
</success_criteria>

<output>
After completion, create `.planning/phases/04-recipe-library/04-01-SUMMARY.md`
</output>
