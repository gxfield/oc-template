---
phase: 04-recipe-library
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - TOOLS.md
autonomous: true
requirements:
  - RCPE-04

must_haves:
  truths:
    - "RSS recipe inspiration in briefings shows only dinner-appropriate recipes, not breakfast/snack/dessert"
    - "Agent filters RSS feed entries before picking random recipes for the briefing"
  artifacts:
    - path: "TOOLS.md"
      provides: "Updated RSS recipe inspiration instructions with dinner-only filter"
      contains: "dinner"
  key_links:
    - from: "TOOLS.md"
      to: "Briefing output"
      via: "Recipe Inspiration step in Data Assembly filters for dinner recipes"
      pattern: "dinner"
---

<objective>
Update the briefing's Recipe Inspiration section to filter RSS feed entries to dinner-appropriate recipes only, excluding breakfast, snack, dessert, and other non-dinner categories.

Purpose: The briefing currently shows random recipes from the RSS feed with no filtering. Users want dinner ideas only, not breakfast muffins or dessert recipes.
Output: Updated TOOLS.md briefing instructions with RSS dinner filter logic.
</objective>

<execution_context>
@/Users/greg/.claude/get-shit-done/workflows/execute-plan.md
@/Users/greg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-recipe-library/04-CONTEXT.md
@TOOLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update briefing RSS recipe inspiration with dinner-only filter</name>
  <files>TOOLS.md</files>
  <action>
In TOOLS.md, update the Briefing section's Recipe Inspiration instructions. There are THREE places to update:

**1. Data Assembly Step 7 (Recipe Inspiration):**

Change from:
```
7. **Recipe Inspiration:** Fetch RSS feed from `https://peaceloveandlowcarb.com/feed/`, extract 2-3 random recipe entries (title + link), format as bullet list.
```

Change to:
```
7. **Recipe Inspiration:** Fetch RSS feed from `https://peaceloveandlowcarb.com/feed/`, filter to dinner-appropriate recipes only, then pick 2-3 random entries (title + link). **Dinner filter:** Exclude entries whose title contains breakfast, snack, dessert, cookie, cake, muffin, smoothie, pancake, waffle, brownie, bar, treat, candy, fudge, dip, appetizer, drink, cocktail, latte (case-insensitive). Include everything else — the feed is primarily low-carb dinner recipes, so filtering out non-dinner keywords is sufficient. If fewer than 2 entries remain after filtering, show what's available.
```

**2. Output Format Template — Recipe Inspiration section:**

Change from:
```
🍳 Recipe Inspiration
• {Recipe Title} — {URL}
• {Recipe Title} — {URL}
{Always show this section. Pick 2-3 random entries from the RSS feed.}
```

Change to:
```
🍳 Recipe Inspiration
• {Recipe Title} — {URL}
• {Recipe Title} — {URL}
{Always show this section. Pick 2-3 random dinner recipes from the RSS feed (exclude breakfast, snack, dessert items).}
```

**3. DO / DO NOT table — update the RSS row:**

Change from:
```
| Fetch 2-3 random recipes from the RSS feed each time | Cache or repeat the same recipes | Variety keeps the section interesting |
```

Change to:
```
| Fetch RSS feed, filter out non-dinner items (breakfast, snack, dessert, cookies, etc.), then pick 2-3 random entries | Show unfiltered recipes or cache/repeat the same ones | User wants dinner inspiration only; variety keeps it interesting |
```

The filter approach is keyword-based exclusion per Claude's discretion (as specified in CONTEXT.md). This is simpler and more reliable for cheap LLMs than trying to positively identify dinner recipes.
  </action>
  <verify>
1. Read TOOLS.md Briefing section
2. Confirm Data Assembly Step 7 mentions dinner filter with exclusion keywords
3. Confirm Output Format Template mentions dinner recipes
4. Confirm DO/DO NOT table row mentions filtering out non-dinner items
5. Verify the rest of the Briefing section is unchanged
  </verify>
  <done>Briefing RSS recipe inspiration instructions filter for dinner-appropriate recipes by excluding breakfast, snack, and dessert keywords</done>
</task>

</tasks>

<verification>
1. TOOLS.md Briefing Data Assembly Step 7 includes dinner filter with exclusion keyword list
2. TOOLS.md Output Format Template mentions dinner recipes
3. TOOLS.md DO/DO NOT table mentions filtering out non-dinner items
4. No other Briefing section content was changed
</verification>

<success_criteria>
- Agent instructions clearly specify that RSS recipe inspiration must filter to dinner-appropriate recipes only
- Filter approach is keyword-based exclusion (simple for cheap LLMs to implement)
- Exclusion keywords cover common non-dinner categories: breakfast, snack, dessert, baking items
</success_criteria>

<output>
After completion, create `.planning/phases/04-recipe-library/04-02-SUMMARY.md`
</output>
